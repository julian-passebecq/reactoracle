apiVersion: v1
kind: Secret
metadata:
  name: reactoracle-data-secrets
  namespace: airflow
type: Opaque
stringData:
  # Do not commit a real token. Prefer kubernetes/airflow/install.sh,
  # which creates this Secret from the local MOTHERDUCK_TOKEN environment variable.
  motherduck-token: REPLACE_AT_DEPLOY_TIME
